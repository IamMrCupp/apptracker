apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: apptracker-data
  labels:
    app.kubernetes.io/name: apptracker
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName: longhorn   # uncomment / set to your cluster's default
